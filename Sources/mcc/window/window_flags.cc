#include "mcc/window/window_flags.h"

namespace mcc {
  DEFINE_int32(window_width, kDefaultWindowWidth, "The default width of the Window, 0 is automatic.");

  DEFINE_int32(window_height, kDefaultWindowHeight, "The default height of the Window, 0 is automatic.");
}